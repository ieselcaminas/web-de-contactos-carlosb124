{# templates/nuevo.html.twig #}
{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(formulario, {'attr': {'novalidate': 'novalidate'}}) }}
    {{ form_widget(formulario) }}
    <button type="submit">Enviar</button>
    {{ form_end(formulario) }}
{% endblock %}
